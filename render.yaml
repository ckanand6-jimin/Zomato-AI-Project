services:
  - type: web
    name: zomato-ai-backend
    env: python
    plan: starter
    branch: main
    buildCommand: pip install -r requirements.txt && python -m data.loader --refresh
    startCommand: uvicorn app.api:app --host 0.0.0.0 --port $PORT
